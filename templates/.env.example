# GraphQL (required when GraphQL stack is enabled)
# Dev placeholder — Countries API; replace with your project endpoint
EXPO_PUBLIC_GRAPHQL_URL=https://countries.trevorblades.com/

# Subscriptions (opt-in at intake — enables WS link + Home subscription demo)
# EXPO_PUBLIC_GRAPHQL_SUBSCRIPTIONS_ENABLED=true
# EXPO_PUBLIC_GRAPHQL_WS_URL=
