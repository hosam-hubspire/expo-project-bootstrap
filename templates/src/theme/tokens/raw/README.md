# Raw token exports

JSON in this folder is **illustrative sample data** so the generator and app shell compile before a real Figma export. During bootstrap, **replace entirely** with this project's Figma variable exports (see [`FIGMA_EXPORT.md`](../../../FIGMA_EXPORT.md)).

After export, run `bun run tokens:generate` from the project root.
